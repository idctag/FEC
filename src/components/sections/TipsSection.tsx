import { TipsType } from "@/blocks/tips/Server";
import React from "react";

const TipsSection = (props: TipsType) => {
  console.log(props);
  return <div>TipsSection</div>;
};

export default TipsSection;
